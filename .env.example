# Alchemy/Cloudflare credentials can come from `bun alchemy login` or env vars.
# Copy this file to `.env` only if your setup needs explicit credentials.
# CLOUDFLARE_ACCOUNT_ID=
# CLOUDFLARE_API_TOKEN=
